name: Build

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build solution
        run: dotnet build CmplPiler.slnx --configuration Release

      - name: Smoke test CLI (list, dry-run)
        run: |
          dotnet run --project CmplPiler.Cli --configuration Release -- examples/hello-cpp/hello.cmpl --list
          dotnet run --project CmplPiler.Cli --configuration Release -- examples/hello-cpp/hello.cmpl -p gcc-release --dry-run

      - name: Build example with gcc via cmpl
        if: runner.os == 'Linux'
        run: |
          dotnet run --project CmplPiler.Cli --configuration Release -- examples/hello-cpp/hello.cmpl -p gcc-release
          ./examples/hello-cpp/build/gcc-release/hello

      - name: Self-host build of the CLI
        run: dotnet run --project CmplPiler.Cli --configuration Release -- examples/self-host.cmpl -p cli-release
