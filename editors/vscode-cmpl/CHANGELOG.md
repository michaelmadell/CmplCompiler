# Changelog

## 0.1.0

Initial release.

- Associates `*.cmpl` files with the YAML language for syntax highlighting.
- Bundles the CMPL JSON schema and applies it via the `yamlValidation`
  contribution point, providing validation, autocomplete and hover docs with
  no configuration.
- Depends on the Red Hat YAML extension (installed automatically).
